spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://ep-bold-bird-azeq43p4-pooler.c-3.ap-southeast-1.aws.neon.tech/neondb?sslmode=require&channelBinding=require}
    username: ${DB_USERNAME:neondb_owner}
    password: ${DB_PASSWORD:npg_zGwkOPft9v5F}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: false

app:
  otp:
    expose-in-response: false
